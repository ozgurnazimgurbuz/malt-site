---
description: Auto-commit and push site changes without asking Özgür
alwaysApply: true
---

# Deploy workflow (Özgür)

- Never ask whether to commit or push.
- When given a prompt/task for this site, implement the required changes, rebuild/prerender if needed, then commit and push to GitHub (`seo-rebuild` + merge to `main`) so Netlify goes live.
- Do not commit `malt-site-full.zip` or secrets.
- Brief status after deploy is enough; no “push edeyim mi?” questions.
